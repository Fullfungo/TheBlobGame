#ifndef GAME_META_H
#define GAME_META_H

enum class CoinFlip{
    Heads, Tails
};

enum class Fairness{
    Fair, Biased
};

enum class PlayerChoice{
    Fair, Biased, Unsure
};

#endif // GAME_META_H
